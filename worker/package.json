{
  "name": "mesh-llm-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker that proxies Bedrock Converse requests for mesh-agent-cli.",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "tail": "wrangler tail",
    "secret:key": "wrangler secret put BEDROCK_API_KEY"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250121.0",
    "typescript": "^5.8.2",
    "wrangler": "^4.84.1"
  }
}
